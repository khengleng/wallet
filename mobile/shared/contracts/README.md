# Mobile Shared Contracts

This folder holds typed contracts used across modules.

Minimum contract families:
- domain events (`wallet_balance_updated`, `session_revoked`, etc.),
- shared API DTO contracts,
- feature manifest schema.

Contract changes should be backward compatible or versioned explicitly.
