# App Shell Baseline

Responsibilities:
- startup orchestration,
- authentication bootstrap/guard,
- feature manifest verification,
- global navigation root and deep-link routing.

Implementation should keep shell code independent from domain module business logic.
